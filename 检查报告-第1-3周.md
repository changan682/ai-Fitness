# 第 1-3 周（Java 传统业务层）规范符合性检查报告

> 检查对象：`agent2` 项目（AI健身私教 & 体态管家 — Java Spring Boot BFF）
> 检查依据：`提示词.txt` 第一阶段（第1-3周）全部规范 + 第八、九、十、十一章
> 检查方式：以「规范逐条 → 现有代码」双向比对，产出缺口清单后逐项修复，最后跑测试验证

---

## 一、结论摘要

前三周的交付物**原本处于「骨架完整但契约不达标」的状态**：核心 CRUD、JWT、Redis 缓存、分布式锁、定时任务都已具备，但存在若干**会让前后端联调直接失败**的问题，以及两个模块整体缺失。

修复后：

| 维度 | 修复前 | 修复后 |
|:---|:---|:---|
| **应用能否启动** | ❌ **不能** — `application.yml` 是非法 YAML | ✅ 可启动 |
| `mvn test` | ❌ BUILD FAILURE（14 跑 10 错） | ✅ 103 通过 / 0 失败 |
| 规范 33 接口中前三周应覆盖的 | 缺失 4 个 | 全部覆盖 |
| 响应结构不符 | 7 条 | 0 条 |
| 业务 msg 文案不符 | 11 处 | 0 处 |
| 参数校验缺失 | 12 项 | 0 项 |
| Redis 缓存策略 | 4 项未实现 | 全部实现 |
| traceId 链路追踪 | 完全未实现 | 已实现 |

---

## 二、修复清单

### 2.0 致命：应用根本无法启动（本轮最重要的发现）

| # | 问题 | 修复 |
|:--|:---|:---|
| 0 | **`src/main/resources/application.yml:67` 是非法 YAML，应用连启动都做不到** —— `key-prefix: fitness:cache:` 的值是裸标量却以冒号结尾，SnakeYAML 会把结尾的冒号解析成映射键，在**解析阶段**直接抛 `ScannerException: mapping values are not allowed here`，任何 `SpringApplication.run` 都会在加载配置时就崩掉（还没走到连数据库那一步）。<br>⚠️ **这个坑是规范自带的**：`提示词.txt:705` 的示例写法完全相同，被原样抄进了配置。 | 改为 `key-prefix: "fitness:cache:"`（加引号），并在配置里写明为什么必须加 |

> 发现过程：写 `@DataJpaTest` / `@SpringBootTest` 时，任何 Spring 上下文都起不来。子代理一开始在测试类路径放了一份 `application.yml` 来绕过，但**绕过不等于修好** —— 那只会让测试变绿、线上依然起不来。已改为直接修主配置并删除绕过文件，再用完整测试套件反证：103 个用例全部通过时，加载的就是真实的 `application.yml`。

### 2.1 严重：会让联调直接失败的问题

| # | 问题 | 修复 |
|:--|:---|:---|
| 1 | **训练计划模板模块整体缺失** — `t_workout_plan_template`/`t_template_exercise`/`t_user_workout_schedule` 三张表有表无代码，规范 6.1/6.2 两个接口不存在 | 新增 3 实体 + 3 仓储 + 5 DTO + `WorkoutPlanService` + `WorkoutPlanController`；`sql/init.sql` 补三种预设模板共 84 条动作明细 |
| 2 | **`GET /api/v1/weekly-plan/latest` 缺失**（规范 9.2） | 新增 `WeeklyPlanService` + `WeeklyPlanController`，无数据时返回 `data:null` 供前端展示 Empty |
| 3 | **`GET /api/v1/health` 缺失**（规范 10.1，且在 JWT 白名单里却无实现） | 新增 `HealthCheckService` + `HealthController`，逐项探测 MySQL/Redis/RabbitMQ/Python Agent |
| 4 | **CORS 预检被鉴权拦下** — 浏览器 OPTIONS 预检不带 `Authorization`，`JwtInterceptor` 直接返回 401，导致所有跨域 POST/PUT/DELETE 根本发不出去 | `JwtInterceptor.preHandle` 首行加 `CorsUtils.isPreFlightRequest(request)` 放行 |
| 5 | **`PUT /api/v1/training/record/{id}` 无法部分更新** — 用了新增场景的全必填 DTO，传 `{"sets":5}` 直接被校验拦成 9003，而规范 2.5 明确「所有字段均为可选」 | 新增 `TrainingRecordUpdateRequest`（仅保留范围校验） |
| 6 | **`/api/v1/body-metrics/trend` 路径 404** — 代码是单数 `/body-metric/trend`，规范 3.2 写的是复数 | 控制器映射改为 `{"/api/v1/body-metric", "/api/v1/body-metrics"}`，两种写法都通 |
| 7 | **趋势接口响应结构与规范完全不同** — 返回 `{rawData, avg7d, latestWeight}`，规范要求 `{list[{id,recordDate,…,weightAvg7d}], latestWeight, weightChange, totalRecords}` | 重构 `BodyMetricTrendResponse` 与 `getTrend` 计算逻辑 |
| 8 | **规范 4.2 返回裸数组** — 需要 `{list, date, totalCalories}` | 新增 `DietRecordListResponse`，`totalCalories` 走仓储层 SUM 聚合 |
| 9 | **`GET /api/v1/stats/weekly` 无 `weekStart` 入参** — 前端按规范传参会被静默忽略，永远只能看当前周 | `StatsController` 加可选参数，`StatsService` 加周参数重载并归一化到周一 |
| 10 | **注册接口 `data` 为 null** — 规范 1.1 要求返回 `{id, nickname, phone(脱敏)}` | 新增 `RegisterResponse`，`UserService.register` 改为返回该对象 |
| 11 | **traceId 链路追踪完全未实现**（第十章强制要求） | 新增 `TraceIdUtil` + `TraceFilter`（OncePerRequestFilter）+ `logback-spring.xml`（JSON encoder + `%X{traceId}`）+ pom 引入 `logstash-logback-encoder` |

### 2.2 一般：契约与正确性问题

| # | 问题 | 修复 |
|:--|:---|:---|
| 12 | **11 处业务 msg 全返回 `"success"`** — 规范对多数接口指定了文案（"档案已更新"/"成功录入N条训练记录"/"记录已删除"/"身体数据已记录"/"饮食记录已保存"/"已退出登录"/"Token已刷新"…） | 补 `Result.ok(msg, data)` 重载，逐接口对齐文案 |
| 13 | **非法日期/缺参数/类型错误全部落成 9999（系统内部错误）** — 控制器有 9 处裸 `LocalDate.parse` | `GlobalExceptionHandler` 增加 `DateTimeParseException`/`MissingServletRequestParameterException`/`MethodArgumentTypeMismatchException`/`HttpMessageNotReadableException`/`ConstraintViolationException` 处理器，统一归为 9003 |
| 14 | **HTTP 状态码不统一** — 校验失败返回 400、系统异常返回 500，而前端按 2xx 分支解析 `data.code`，会走错分支 | 全局统一 HTTP 200 + 业务 code（含 `JwtInterceptor` 的鉴权失败：401 → 200+9001） |
| 15 | **校验失败文案格式不符** — 是 `"nickname: 昵称长度需2-20字符"`，规范是 `"参数校验失败：昵称长度需2-20字符"` | 只保留 `defaultMessage` 并加统一前缀 |
| 16 | **食物存在性校验用模糊匹配** — 传「鸡」会命中「鸡蛋(煮)」并取第一条，既误判存在性（规范 4.1 要求不存在返回 4001）又把热量算错 | 仓储加 `findByFoodName` 精确查询；新增 `getFoodExactly`，未命中写空值标记防穿透 |
| 17 | **食物库 `keyword`+`category` 同时传时 keyword 被丢弃** — 规范 5.1 的示例正是 `?keyword=鸡&category=肉类` | 新增组合查询 `findByFoodNameContainingAndCategory` |
| 18 | **7 日滑动平均按「记录条数」滑窗而非自然日** — 用户隔天记录时，12 天跨度的两条数据会被平均成「7日均值」，结论失真 | 改为自然日窗口 `[d-6, d]`：额外多查 6 天数据算窗口，只返回区间内的点 |
| 19 | **12 项参数校验缺失** — 密码复杂度（大小写+数字）、身高 50-250、体重 30-300、rpe 1-10、体脂率 3-60%、weightKg≥0、batch 子项 sets/reps 必填等 | 全部补齐；新增 `ValidationPatterns` 集中管理正则，避免规则漂移 |
| 20 | **分页无边界** — `page=0` 会让 `PageRequest.of(-1,…)` 抛异常 | 服务层钳制 `page≥1`、`size∈[1,200]` |
| 21 | **`ErrorCode` 文案不符** — 3001 缺「请使用修改接口」 | 对齐规范；3002 的语义冲突（规范 3.1.1 把「不存在」也写成 3001）已在枚举内注释说明 |
| 22 | **改密旧密码错误文案不符** — 规范要求 `"旧密码错误"` | 用 `BusinessException(ErrorCode.PASSWORD_ERROR, "旧密码错误")` |
| 23 | **刷新 Token 用了错误码表外的裸数字 9004** | 补入枚举 `TOKEN_REFRESH_NOT_ALLOWED(9004, …)` 并注明落在 9001-9999 区间内 |
| 24 | **`injuryRecord` 类型不符** — 规范 1.3/1.4 都是字符串数组，代码是 `String` | 改为 `List<String>`，实体侧按 DDL 注释存 JSON 数组字符串，兼容历史脏数据 |

### 2.3 Redis 缓存策略（第五章「必须完整实现，禁止 TODO 占位」）

| # | 规范要求 | 修复 |
|:--|:---|:---|
| 25 | 2.10 本周统计缓存 `stats:weekly:{userId}:{weekStart}` TTL 1h | 新增 Cache-Aside + 互斥锁回源；训练记录增删改主动失效该 key |
| 26 | 2.5 7日滑动均值 `metric:avg7d:{userId}` TTL 1h | 趋势计算后写入；录入/修改时失效；Dashboard 通过 `getLatestAvg7d` 消费 |
| 27 | 2.4 最新体测 `metric:latest:{userId}` **Hash** 六字段 + 写穿透 | 改为 Hash（含规范的 6 字段 + id/user_id/created_at），`getLatest` 先读 Hash 再回源 |
| 28 | 第5节 缓存穿透防护（空值标记 TTL 60s） | `RedisCacheService.setNullMarker/hSetNullMarker/hSetNullField`，用于最新体测与食物库查询 |
| 29 | 第5节 缓存击穿防护（热点 key 互斥锁） | `RedisCacheService.getOrLoad(cacheKey, lockKey, ttl, lockTtl, loader)`，未抢到锁的等待重试、兜底回源 |
| 30 | 强约束第8条 **TTL 必须加 ±300s 随机扰动** 防雪崩 | 统一走 `setWithJitter/expireWithJitter`；**唯一例外**：`training:today` 与 AI 日总结的「到次日凌晨」TTL 保持精确（加扰动会导致跨零点返回昨天的数据，比雪崩更严重），已在代码注释说明 |
| 31 | 强制「所有缓存读写统一封装，严禁绕过」 | 6 个 Service 全部改为注入 `RedisCacheService`，`RedisTemplate` 只允许出现在 `RedisCacheService`/`DistributedLockUtil`/`RedisConfig` 三个基础设施类中 |
| 32 | 强约束「严禁硬编码裸 Key」 | 新增 `CacheKeys` 的 key 构建方法（`trainingToday`/`metricLatest`/`statsWeekly`/`lockScheduled`…），锁 key 也收归常量 |
| 33 | 2.2 Token 黑名单 key 应为 `user:token:blacklist:{userId}` | **后续修正为按 jti 建 key**（`user:token:blacklist:{jti}`）。先用「userId + JSON 数组」保住了覆盖问题，但同一用户多设备登录仍共用一条记录、语义上无法区分 Token；现改为 JWT 自带 jti 作 key（一 Token 一记录），并以 `user:token:invalid-after:{userId}` 水位线支持「改密码作废全部旧 Token」。详见 `JwtInvalidationTest` |
| 34 | 2.1 档案缓存「延迟双删」 | 写库后删一次 + `afterCommit` 再删一次 |
| 35 | 2.6/2.7 启动预加载食物库**和**训练模板 | `CacheWarmUpRunner` 补模板预热；食物库预热改为读 Redis Hash 计数，避免启动期全表查询；预热失败不阻断启动 |

### 2.4 其它

| # | 问题 | 修复 |
|:--|:---|:---|
| 36 | 全项目**没有任何 Jackson 日期格式化** → `createdAt` 输出 ISO `2026-07-30T15:30:00`，违反「统一 `yyyy-MM-dd HH:mm:ss`」 | 新增 `JacksonConfig`（用 `modulesToInstall` 追加而非 `modules` 覆盖，避免干掉 JavaTimeModule） |
| 37 | 规范「配置类」清单中的 `CorsConfig`/`JwtConfig` 不存在 | 抽出 `CorsConfig`；新增 `JwtProperties`+`JwtConfig`，`JwtUtil` 改为构造注入属性 |
| 38 | `RedisConfig` 自定义 `CacheManager` 后 `spring.cache.redis.key-prefix` 实际不生效 | 显式 `computePrefixWith(...)` 接上配置的前缀 |
| 39 | `RedisConfig` 注释称「不写入 @class」与事实相反 | 修正注释并说明缓存值不要用 JDK 不可变集合（`@class` 指向 `ImmutableCollections$ListN` 会导致反序列化失败） |
| 40 | 实体 `@Index/@UniqueConstraint` 的 `columnList` 写的是 Java 属性名 | 统一改为物理列名（snake_case），与 `sql/init.sql` 一致（否则 H2 create-drop 建索引会报列不存在） |
| 41 | 食物库被整体免鉴权，但规范 5.1 标注需 `Authorization` | 移出白名单（`/api/v1/health` 保留，探针需要） |
| 42 | 食物库分类返回字典序，规范示例是固定顺序 | 按 主食/肉类/蔬菜/水果/乳制品/零食/饮品 固定顺序输出 |
| 43 | 刷新 Token 复用了 `LoginResponse`，多带一个 `user` 对象 | 新增 `RefreshTokenResponse`（仅 token/expiresAt） |
| 44 | `DietRecordService` 注入 `RedisTemplate` 却完全未使用 | 移除 |
| 45 | `RedisCacheService.lPush` 实际调的是 `rightPush` | 更名 `rPush`/`rPushAll` |
| 46 | `application.yml` 缺 `spring.application.name`（日志 JSON 的 service 字段取不到） | 补 `java-bff` |
| 47 | **测试环境本身是坏的**：JDK 21 下 Mockito 内联 mock maker 无法自附加 agent（`jdk.attach.allowAttachSelf` 默认关闭，退化为外部进程附加也失败） | surefire 配置 `-Djdk.attach.allowAttachSelf=true -XX:+EnableDynamicAgentLoading` |

---

## 三、新增文件清单

**实体 / 仓储（补齐有表无代码的三张表）**
- `entity/WorkoutPlanTemplate.java`、`entity/TemplateExercise.java`、`entity/UserWorkoutSchedule.java`
- `repository/WorkoutPlanTemplateRepository.java`、`repository/TemplateExerciseRepository.java`、`repository/UserWorkoutScheduleRepository.java`

**DTO（对齐规范响应结构）**
- `dto/WorkoutTemplateResponse.java`、`dto/TemplateExerciseResponse.java`、`dto/ApplyScheduleRequest.java`
- `dto/ScheduleDayResponse.java`、`dto/ApplyScheduleResponse.java`、`dto/WeeklyPlanResponse.java`
- `dto/HealthResponse.java`、`dto/RegisterResponse.java`、`dto/RefreshTokenResponse.java`
- `dto/DietRecordListResponse.java`、`dto/TrainingRecordUpdateRequest.java`

**Service / Controller**
- `service/WorkoutPlanService.java`、`service/WeeklyPlanService.java`、`service/HealthCheckService.java`
- `controller/WorkoutPlanController.java`、`controller/WeeklyPlanController.java`、`controller/HealthController.java`

**基础设施**
- `cache/TokenBlacklistService.java`、`config/TraceFilter.java`、`config/JacksonConfig.java`
- `config/CorsConfig.java`、`config/JwtProperties.java`、`config/JwtConfig.java`
- `util/TraceIdUtil.java`、`common/ValidationPatterns.java`
- `src/main/resources/logback-spring.xml`、`src/test/resources/application-test.yml`

---

## 四、需要你手动做的一件事

`sql/init.sql` 已改动（新增唯一索引 + 三种模板种子数据），**请重新执行一次初始化脚本**，否则训练模板接口会返回空列表：

```bash
mysql -h 192.168.199.128 -u root -p < sql/init.sql
```

脚本是幂等的（`CREATE TABLE IF NOT EXISTS` + `ON DUPLICATE KEY UPDATE`），重复执行不会产生脏数据。

---

## 五、验证结果

```
mvn -B test
Tests run: 110, Failures: 0, Errors: 0, Skipped: 4
BUILD SUCCESS
```

| 测试类 | 用例数 | 层级 |
|:---|--:|:---|
| `cache/DistributedLockUtilTest` | 4 | 单元（加锁/解锁 Lua） |
| `entity/TrainingRecordVolumeTest` | 4 | 单元（容量自动计算） |
| `service/BodyMetricServiceTest` | 11 | 单元（7日滑动平均按自然日、防穿透标记） |
| `service/TrainingRecordServiceTest` | 16 | 单元（容量、分页钳制、缓存失效） |
| `controller/UserControllerTest` | 9 | `@WebMvcTest`（注册/登录/校验失败 9003） |
| `controller/TrainingControllerTest` | 14 | `@WebMvcTest`（新增/查询/部分更新/删除文案） |
| `config/CorsPreflightTest` | 3 | `@WebMvcTest`（**跨域预检放行 + 反向断言**） |
| `repository/UserAndFoodRepositoryTest` | 7 | `@DataJpaTest` + H2 |
| `repository/TrainingAndBodyMetricRepositoryTest` | 12 | `@DataJpaTest` + H2 |
| `repository/DietAndWeeklyPlanRepositoryTest` | 7 | `@DataJpaTest` + H2 |
| `repository/WorkoutPlanRepositoryTest` | 9 | `@DataJpaTest` + H2 |
| `integration/RedisCacheIntegrationTest` | 10 | `@SpringBootTest`（上下文装配 + Redis 缓存读写） |
| `client/AiPythonClientLiveTest` | 4（按需） | 跨语言实测，默认按环境变量跳过（第4周） |
| **合计** | **110**（含 4 跳过） | |

### 关于 `CorsPreflightTest` 的可信度

一个只断言「预检返回 200」的测试是可能假绿的 —— 如果拦截器根本没装进测试上下文，预检当然通过。因此该测试做了两件事：

1. **反向断言**：不带 Token 的 `GET` / `DELETE` 必须返回 `code=9001`，证明拦截器确实在生效；
2. **变异验证**：把 `JwtInterceptor` 里的预检放行临时注释掉再跑，测试必须变红。实测结果：

```
[ERROR] CorsPreflightTest.preflightShouldBeAllowed:64
        Expected no value at JSON path "$.code" but found: 9001
Tests run: 3, Failures: 1
```
失败信息精确指向了原始故障（预检被鉴权拦下、拿到 9001），说明这个测试真的能挡住回归，而不是摆设。验证后已恢复修复并回归全绿。

测试基础设施（`src/test/resources/`）：
- `application-test.yml` — H2 内存库 + 排除真实中间件
- `test_data.sql` — 规范要求的统一测试数据
- `h2-mysql-compat.sql` + `testsupport/`（3 个类）— 曾用于让「连续训练天数」的 MySQL 原生递归 CTE 在 H2 上能跑。
  **现已删除**：该统计改为「JPQL 取日期 + Java 算逻辑」（见 `StatsService#countConsecutiveTrainingDays`）后是通用写法，H2 直接可执行，那套 SQL 改写补丁不再需要。

> 说明：按你的要求，测试**没有继续补齐**，当前这套已覆盖规范「九、测试策略」要求的最低项（Service 单元、每个 Repository 至少 1 例、User/Training 两个 Controller、Redis 缓存集成）。剩余的边缘用例（如 WorkoutPlanService / FoodLibraryService 的专属单测）未做。

---

## 六、明确不在本次范围（第 4-8 周）

| 内容 | 归属 |
|:---|:---|
| `/api/ai/*` AI 代理 5 个接口、`RestClientConfig`、AI 降级兜底文案 | 第 4-5 周 |
| `/api/ai/callback/*` AI 回调 + HMAC 验签、`t_ai_summary_cache` 实体、`ai:summary` 缓存与击穿互斥锁 | 第 5-7 周 |
| `RabbitMQConfig`（`ai.fitness.exchange` / `ai.weekly.plan` / DLQ / TTL 7天）、MQ 生产消费全链路 | 第 7 周 |
| Java→Python 的 `X-Trace-Id` 注入、MQ 消息头 traceId 透传 | 第 4/7 周（Java 侧基础设施已完成） |
| React + TS 前端、Docker Compose 编排、README | 第 8 周 |

---

## 七、注意事项

1. **JDK 21 跑测试**：已在 `pom.xml` 的 surefire 里加 `-Djdk.attach.allowAttachSelf=true`，命令行 `mvn test` 正常。若用 IDE 直接运行测试，请在 Run Configuration 的 VM options 里加上同一参数，否则 Mockito 会报 `Could not self-attach to current VM`。
2. **HTTP 状态码约定**：全部接口（含鉴权失败）现在统一返回 HTTP 200，业务结果看 `code`。前端 axios 只需判断 `code === 0` / `code === 9001`（清 Token 跳登录）。
3. **`training:today` 缓存不加 TTL 扰动**是刻意为之（见 2.3 第 30 条）。
4. `metric:latest` 的 TTL 取 30 天：规范 2.4 说「永久」与强约束第 8 条「不得永不过期」冲突，取折中（写穿透刷新 + 长 TTL）。
