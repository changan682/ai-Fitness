# 第 8 周：React+TS 前端联调 + Compose 编排 + 文档收尾

> 本周目标（规范第 749 行）：**全量接口与 React+TS 前端联调，Docker Compose 编排（含 Milvus 容器），编写 README**。
> 验收标准：**前端页面跑通全流程，AI 对话可检索知识库**。

---

## 一、本周交付

| 交付物 | 位置 | 说明 |
|:---|:---|:---|
| React+TS 前端 | `fitness-frontend/` | 5 个页面、33 个接口全部接通 |
| 前端类型层 | `fitness-frontend/src/types/` | 与后端 DTO/实体**逐字段**对齐（含 Map 型接口） |
| 请求层 | `fitness-frontend/src/api/` | Axios 单例 + 拦截器 + 9 个模块 |
| 状态层 | `fitness-frontend/src/store/` | Zustand：登录态 / 今日训练本地状态 |
| Java 生产镜像 | `Dockerfile` + `.dockerignore` | 基于 `eclipse-temurin:17-jre` |
| Python 生产镜像 | `python-agent/Dockerfile` + `docker-entrypoint.sh` + `.dockerignore` | 等 Milvus 就绪后再起 FastAPI |
| Compose 编排 | `docker-compose.yml` | 默认只起 Milvus；新增 `local-mw` / `apps` 两组可选 profile |
| 文档 | 根 `README.md`、`fitness-frontend/README.md`、本文件 | — |
| 前端测试 | `fitness-frontend/src/**/*.test.ts(x)` + `vitest.config.ts` + `src/test/setup.ts` | 21 项：页面渲染冒烟 + 拦截器逻辑（不依赖后端） |

---

## 二、前端架构

### 2.1 分层

```
页面 pages/            ← 只做布局与交互编排
  ↓
组件 components/       ← 按功能域拆分（auth / dashboard / ai / layout / common）
  ↓
状态 store/ + 请求 api/ ← Zustand（客户端状态）+ TanStack Query（服务端状态）
  ↓
类型 types/            ← 单一事实来源，全部从 @/types 导出
```

**边界规则**：页面不直接 `import axios`，组件不直接拼 URL；所有请求都走 `api/` 里的模块，
所有类型都从 `@/types` 取。这样后端改字段时只需改一处。

### 2.2 数据流（以训练看板为例）

```
DashboardPage
 ├─ useQuery(['stats','dashboard'])  ──▶ GET /api/v1/stats/dashboard   → StatsCards
 ├─ useQuery(['training','today'])   ──▶ GET /api/v1/training/today    → trainingStore（镜像）
 ├─ useQuery(['workout','templates'])──▶ GET /api/v1/workout/templates → 动作名 AutoComplete 候选
 └─ useMutation(批量新增/修改/删除)
      ├─ onMutate : 先改 trainingStore（乐观更新，占位行用负数 id）
      ├─ onError  : 恢复 mutation 前的整表快照 + 提示
      ├─ onSuccess: 用后端返回的权威记录（含后端算的 volume）替换占位行
      └─ onSettled: invalidateQueries(今日记录 + Dashboard 统计)
```

### 2.3 与后端的关系

Vite 把 `/api` 代理到 `http://localhost:8080`（见 `vite.config.ts`）。
前端**不感知** Python / FastAPI / Milvus —— 这是设计约束，不是疏漏：
跨语言与 AI 的复杂度全部收在 Java 的 BFF 层。

---

## 三、关键设计决策

### 3.1 错误处理：全部在「响应成功」分支里

后端**业务失败也返回 HTTP 200**（鉴权失败 9001 亦然，由 `JwtInterceptor` 显式写出 200）。
因此：

- `catch` 分支只处理**网络层**问题：断网、超时（15s）、5xx
- 业务错误在响应拦截器里按 `code` 分流，并拆掉信封 `{code,msg,data}`

分流表见 `fitness-frontend/README.md` 第 4.1 节。要点是**分级**：

- 全局处理：9001（清态 + 跳登录，**2 秒去抖**防止并发请求弹一串提示）、9002、9003
- 静默交给页面：1002/1003（登录页要把错误放在密码框下）、2003（AI 总结页要用 `Result` 展示）、
  3001（体测表单）、4001（饮食表单）、6001-6003（AI 兜底文案卡片）

> 若不静默，用户会看到「拦截器弹一次 + 页面再弹一次」的重复提示。

### 3.2 Token：静默刷新 + 记住我

- 拦截器解析 JWT 的 `exp`：剩余 **< 24h** 时先调 `/v1/user/refresh`，成功再发原请求
- 并发请求共享同一个刷新 Promise；刷新请求走**无拦截器的裸实例**，避免递归
- 刷新失败不阻断原请求（真失效了由 9001 兜住）
- 「记住我」决定落 `localStorage` 还是 `sessionStorage`：用一个 2 秒去抖的
  `localStorage` 标记 + 上次登录手机号，登录页自动回填

### 3.3 路由与鉴权

```
RootShell（挂 AppBridge）→ ProtectedRoute（查 isAuthenticated）→ AppLayout（侧边栏+内容区）→ 页面
```

- 未登录时**连布局都不渲染**，不会出现「侧边栏 + 登录页」的怪状态
- `ProtectedRoute` 把来源路径写进 `location.state.from`，登录成功后回跳原页面
- 启动时在 `main.tsx` **同步**调用 `restoreSession()`：若放到页面 effect 里，
  `ProtectedRoute` 首帧会误判未登录，表现为「刷新页面就被登出」

### 3.4 响应式布局

桌面用可折叠 `Sider`；移动端（<768px，`Grid.useBreakpoint()` 判定）改用 `Drawer` + 顶部汉堡按钮
—— 固定侧栏在手机上会挤掉正文宽度，且 antd 折叠态仍占 80px。

### 3.5 AI 问答页：四个 Tab 的取舍

- 对话历史只放组件 `useState`，并给 `Tabs` 开 `destroyOnHidden`：
  规范要求「切换 Tab 或离开页面不保留」（避免敏感对话残留）
- 代价是切回其它 Tab 时上一次的生成结果也会丢 —— 对「可随时重新生成」的功能可接受
- 引用来源用 `Collapse` 折叠展示，并**显示真实余弦相似度**，便于判断检索质量

### 3.6 体重有两套口径（最容易搞混）

| 字段 | 含义 | 更新时机 |
|:---|:---|:---|
| `t_user.weight` | 注册时的**初始体重** | 仅手动改档案时 |
| `t_body_metric.weight_kg` | 日常**跟踪体重** | 每次体测录入 |

Dashboard / Trends 的「最新体重」**始终取 `t_body_metric`**；两者独立维护、不自动同步。
Profile 页面把这两件事拆成两个互不影响的表单，并明确标注「最新体测体重」。

---

## 四、编排（Compose + Dockerfile）

### 4.1 默认行为没有改变

`docker compose up -d` **仍然只启动 Milvus（含 etcd + MinIO）**。
MySQL / Redis / RabbitMQ 在虚拟机 `192.168.199.128`，Java 与 Python 在 IDE 里跑
（方便打断点）—— 这是项目一贯的开发方式，本周没有改动它。

### 4.2 新增两组可选 profile

| profile | 服务 | 用途 |
|:---|:---|:---|
| `local-mw` | mysql:8.0 / redis:7-alpine / rabbitmq:3.13-management | 没有那台虚拟机时在本地起中间件 |
| `apps` | java-bff / python-agent | 把两个应用也容器化（需先 `mvn -DskipTests package`） |

```powershell
docker compose --profile local-mw up -d
docker compose --profile local-mw --profile apps up -d   # 两者可叠加
```

用 profile 而不是直接加服务，是为了**不改动**上面那套默认工作流。

**这同时修掉了一个文档与实现不符的问题**：`application-docker.yml` 的注释写着
「中间件全部通过 docker-compose 在本地启动」，但 compose 文件里根本没有
MySQL/Redis/RabbitMQ —— 那个 profile 照文档做是跑不起来的。现在有了 `local-mw`，它成立。

几处刻意的细节：

- **MySQL 挂载 `./sql/init.sql` 到 `docker-entrypoint-initdb.d/`**：首次初始化数据卷时自动建表，
  等价于 README 里的手工 `mysql < sql/init.sql`
- **MySQL 显式设 `--default-time-zone=+08:00`**：MySQL 8 默认 UTC，
  不设会与 `application.yml` 的 `serverTimezone=Asia/Shanghai` 差 8 小时
- **Redis 必须 `--requirepass`**：Java 侧缓存/分布式锁/Token 黑名单都按「有密码」配置，
  空密码会直接 `NOAUTH`
- **RabbitMQ 显式设 vhost `/agent1`**：vhost 是独立于账号的命名空间，
  Java 与 Python 两侧默认都用它

### 4.3 两个 Dockerfile 与规范示例的差异

| 项 | 规范示例 | 实际 | 原因 |
|:---|:---|:---|:---|
| Java 基础镜像 | `openjdk:17-slim` | `eclipse-temurin:17-jre` | 官方 `openjdk` 镜像已停止维护，temurin 是其继任者 |
| Java jar 名 | `fitness-app-1.0.0.jar` | `fitness-server-1.0.0.jar` | 必须与 `pom.xml` 的 artifactId 一致，照抄规范会 `COPY` 失败 |
| Python 基础镜像 | `python:3.10-slim` | `python:3.12-slim` | 与本地实测环境、README「Python 3.12 推荐」一致，少一处未验证组合 |
| 等待 Milvus 的方式 | `pymilvus connections.connect` 轮询 | 纯 socket 探测 | 不依赖 pymilvus 在等待阶段就能正确初始化；失败信息更明确，且超时可配（默认 180s） |

两个镜像都设了 `TZ=Asia/Shanghai`：不设会按 UTC 输出日志时间戳，
与 MySQL 的 `Asia/Shanghai`、JVM 的 `user.timezone` 不一致，排查问题时会看错时间。

安全上：`.dockerignore` 明确排除 `.env` 与 `data/.kb_manifest.json`，
所有密钥通过环境变量在运行时注入，**镜像里不留任何真实值**。

---

## 五、与规范的偏差汇总（都是有意的）

| # | 规范原文 | 实际实现 | 原因 |
|:---|:---|:---|:---|
| 1 | 快捷录入每行是「第 N 组」（重量/次数/RPE） | 每行是一个**动作条目**（动作名/组数/次数/重量/RPE），走批量接口 | 后端一条记录 = 一个动作 + 一个 `sets` 标量，没有「一组一行」的模型。逐组建记录会让列表碎成「1组×10次」；强行合并会丢各组不同重量 |
| 2 | 注册页「手机号失焦调用接口判断是否已注册」 | 失焦只校验格式；提交时后端返回 1001 再提示并切到登录 Tab | 后端**没有**手机号查重接口，照字面实现只能调注册接口 —— 那会真的创建用户 |
| 3 | Token 用 zustand `persist` 持久化 | 自建 `utils/tokenStorage` | persist 在创建时固定 storage，无法按「记住我」在运行期二选一 |
| 4 | store 中 `user: UserProfile` | `user: UserBrief` | 登录接口只返回 4 个字段；声明成 UserProfile 会让页面读 `phone/height` 时类型检查通过、运行时 `undefined` —— 类型说谎 |
| 5 | AI 接口沿用全局 15s 超时 | AI 单独放宽（普通 60s、姿态评估 90s） | 姿态评估走真实多模态，实测可达数十秒；15s 会让用户只看到「请求超时」 |
| 6 | 行内编辑可改动作名 | 只开放组数/次数/重量/RPE | 后端 `TrainingRecordUpdateRequest` 没有 `actionName` 字段 |
| 7 | 趋势页「每周容量」 | 按周逐个请求 `/v1/stats/weekly`，区间上限约 13 周 | 后端没有多周聚合接口；不加限制会在 90 天区间一次打出十几个请求 |
| 8 | `docker-compose.yml` 含 MySQL/Redis | 放进 `local-mw` profile | 保持默认 `up -d` 只起 Milvus 的既有工作流不变 |
| 9 | 容器编排含前端 | 前端仍用 Vite dev server | 规范未要求前端镜像；第 6 节已给 dev 启动方式，生产可自行 `npm run build` 后用 nginx 托管 |

---

## 六、验证方式与实测结果

```powershell
# 1) 前端：严格模式类型检查（0 错误才算过）
cd fitness-frontend; npm run typecheck

# 2) 前端：生产构建（tsc + vite build）
npm run build

# 3) 前端联调：先起 Java(8080) 与 Python(8000)，再起 npm run dev，用浏览器走一遍
#    登录 → 看板录入 → AI 四个 Tab → 趋势 → 档案

# 4) 后端回归（本周未改后端逻辑，用于确认没有连带破坏）
cd ..; mvn test
cd python-agent; E:\Anaconde\python.exe -m pytest tests -q

# 5) 编排：语法与 profile 结构校验
docker compose config                          # 默认只有 etcd/minio/milvus
docker compose --profile local-mw --profile apps config
```

### 6.1 实测结果

| 项 | 结果 |
|:---|:---|
| `npm run typecheck` | **0 错误**（`strict` + `noUnusedLocals` + `noUnusedParameters` 全开） |
| `npm test`（vitest + jsdom） | **21 / 21 通过**（页面渲染冒烟 9 项 + 拦截器逻辑 12 项） |
| `npm run build` | **成功**，4033 个模块，约 29s（新增测试文件未进包，产物逐字节不变） |
| 构建产物 | 入口 `index` **71.5 kB**（gzip 27.4）+ `react-vendor` 61 kB + `antd-vendor` 1.29 MB + `query-vendor` 104 kB + `markdown-vendor` 157 kB；**`chart-vendor`（ECharts，1.05 MB）被拆成独立分片并配合懒加载，只有打开趋势页才下载** |
| 后端回归 | Java **206 项**（`BUILD SUCCESS`）、Python **392 项**全部通过；`git diff 4126fb2..HEAD` 确认**第 8 周的提交只改了前端/编排/文档**，后端源码零改动 |

### 6.2 前端联调：24 项全部通过

验证方式：**所有请求都经 Vite dev server 的 `/api` 代理**（`http://localhost:5173/api`），
即前端在浏览器里实际走的同一条路径；逐条断言业务码与**返回字段名**。
（该轮在最后一次代码修正**之后**重跑，验收对象即当前提交状态。）

| 分组 | 覆盖 |
|:---|:---|
| 代理与健康 | `/api` 正确转发到 Java；四项依赖全 `UP`（mysql/redis/rabbitmq/pythonAgent） |
| 登录页 | 注册 → 登录；断言登录返回的 `user` 字段**恰为** `{id,nickname,gender,trainingGoal}`（印证 4.5 节用 `UserBrief` 而非 `UserProfile` 的判断） |
| 档案页 | 读档案（含 `injuryRecord` 数组、脱敏手机号、`yyyy-MM-dd HH:mm:ss` 时间）→ 改档案 |
| 训练看板 | 模板（动作名候选字段齐全）→ 单条录入（**后端算出 volume=2400**）→ 批量录入（`count=2`）→ 今日列表 → dashboard（8 个字段齐全） |
| 体测与趋势 | 体测录入 → 最新体测 → 趋势（每点含 `weightAvg7d`）→ 周统计（`totalVolume/trainingDays/topActions`）→ 周计划 |
| 饮食（P0 复验） | 食物库检索命中 3 条 + 7 个分类；150g 鸡胸肉热量 **199.5 kcal** 正确 |
| 空周容错 | `?weekStart=2020-01-06`（无记录的一周）返回 `code=0` 且 `totalVolume=0` —— 经验性确认趋势页的 `Promise.all` 不会因空周而整图失败 |
| **AI 四个 Tab** | 训练总结（219 字，`cached=false`）；动作推荐（5 条，字段齐全）；姿态评估（**真实多模态** `score=45`、3 问题 3 建议）；**知识库问答 633 字 + 5 条来源，最高相似度 0.7511**；知识库健康（200 条 / 768 维 / `IVF_FLAT(COSINE, nlist=16)`） |

> 第 7 条即规范第 749 行的验收标准「**AI 对话可检索知识库**」：问答返回了真实 Milvus 检索到的
> 5 条来源及其余弦相似度，而**不是**内置兜底知识（后者分数是合成值）。

### 6.3 逐字段契约校对（对 42 个后端映射）

前端不是「照着规范写类型」，而是**照着 Java 源码逐字段核对**。校验下来发现 5 处
「类型比后端更宽松或更严格」的问题，已全部修正（都尚未被页面调用，属潜在隐患）：

| # | 问题 | 修正 |
|:---|:---|:---|
| 1 | 快捷录入**行数无上限**，而后端 `TrainingBatchRequest.records` 带 `@Size(max = 20)` | 到 20 行即禁用「添加一组」并显示计数（原实现要填满 21 行才撞 9003，是本轮唯一的可用性缺陷） |
| 2 | `WeeklyPlan` 的 `weekStart/suggestionText/createdAt` 声明为非空，后端全是包装类型 | 改为可空（否则调用方 `dayjs(null)` 会拿到 Invalid Date 却误以为是解析问题） |
| 3 | `ApplyScheduleResponse.scheduleId` 声明为非空 | 改为可空（模板无动作时后端返回 null） |
| 4 | `ActionHistoryResponse.maxWeight/maxVolume` 声明为 `number \| null` | 改为非空（后端无记录时返回 0 而非 null） |
| 5 | `KnowledgeHealth` 各字段声明为非空 | 改为可空，并注明：**Milvus 不可用时后端返回 `code=6003` 且整个 `data` 为 null**，而不是规范示例写的 `data:{milvusConnected:false}` ——必须按 `code` 判断 |

**一处刻意不采纳的实现建议**：常见的「9004 → 再调一次 refresh 然后重试」写法是反的。
9004 = `TOKEN_REFRESH_NOT_ALLOWED`，含义是「剩余有效期仍 >24h 或已刷过，无需刷新」，
按那样实现会在不需要刷新时发起刷新、拿到 9004 再重试，形成无意义往返。
本实现只在**剩余有效期 < 24h** 时刷新（与 Java 的 `REFRESH_WINDOW_MS` 一致），
且刷新走不带拦截器的裸实例，即使真收到 9004 也不会弹全局错误。

**一处被证伪的担忧**：趋势页的每周容量用 `Promise.all` 并发取各周统计，
曾担心「某周无记录」会让接口报错、进而使整张图退化成失败态。
核对 `StatsService.computeWeeklyStats`（`StatsService.java:178-199`）后确认：
空周返回的是 `code=0` 且 `trainingDays/totalActions/totalVolume` 全为 0
（`totalVolume` 用 `BigDecimal.ZERO` 作 reduce 单位元，空集合安全），
且该 Service 内**没有任何 `throw new BusinessException`** —— 因此不会 reject，只会画出 0 高柱子。

### 6.4 自动化测试补上了「页面能不能渲染」这一层

初版验证只到「类型对 + 能打包 + 接口契约通」，但**页面渲染本身没有验证**——
环境里没有浏览器，`tsc` 与 `vite build` 又都抓不到首帧运行时错误
（Hook 用错位置、`App.useApp()` 取不到 Context、解构了 `undefined` 的响应……），
这类问题在浏览器里表现为整页白屏而构建是绿的。

因此补了 vitest + jsdom 的两层测试（**不依赖后端**，可进 CI）：

| 层 | 覆盖 |
|:---|:---|
| 页面渲染冒烟（9 项） | 5 个页面 + 404 在 jsdom 里真实挂载并断言关键内容（看板断言到统计卡片文案与打桩数据里的动作名、AI 助手断言到四个 Tab）；`ErrorBoundary` 兜住渲染期异常并展示兜底 UI；`ProtectedRoute` 未登录时重定向、已登录时放行 |
| 拦截器逻辑（12 项） | 信封拆解与非信封响应；错误码分级（9001 清态跳登录且**并发三次只触发一次**、9002/9003/未知码各自的提示方式、1002 与 6001 **静默**）；断网/超时/5xx 三种网络错误文案 |

写测试时发现的一个**测试自身的 bug**（顺手记下，避免后人重踩）：`MemoryRouter` 默认从 `/`
开始，而 `/` 既不匹配 `/dashboard` 也不匹配 `/login`，于是路由用例「什么都不渲染」并以
「找不到元素」失败 —— 看起来像路由守卫坏了，其实是用例没给初始路径。

代价与边界：ECharts 依赖 canvas，jsdom 没有，测试里被替换成空组件，
因此**图表画出来的样子与点击交互仍未被断言**（见 6.5）。

### 6.5 未能在本会话内执行的项

- `docker compose config`：本机 Docker CLI 在当前受限环境下无法访问 Docker API
  （`permission denied ... dockerDesktopLinuxEngine`）。已改用 YAML 解析确认
  **默认启动集合为 etcd/minio/milvus**，且 `local-mw`/`apps` 两组 profile 归属正确。
- 浏览器内的真实点击流与视觉呈现：环境无浏览器自动化工具。已用「页面渲染冒烟 +
  经前端代理逐接口断言」把可自动化的部分都覆盖了，**剩下的是图表外观、响应式断点、
  真实拖拽上传这类必须人眼确认的部分**，建议起服务后人工过一遍。

---

## 七、已知限制与后续可做

| 项 | 说明 |
|:---|:---|
| 前端无自动化测试 | 规范对前端只要求「页面跑通全流程」；质量由 `tsc` 严格模式 + 生产构建 + 联调冒烟覆盖。若要补，优先补拦截器（错误码分流、Token 刷新去抖）的单测 |
| 趋势页请求数 | 每周一次统计请求，90 天区间约 13 次；后端若加多周聚合接口可降为 1 次 |
| 前端未容器化 | 见偏差 #9 |
| 死信告警渠道 | 仍是 ERROR 日志（规范要求「队列深度 >0 打印 ERROR 并告警」已满足前半句，邮件/钉钉为预留） |
